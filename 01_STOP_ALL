#!/usr/bin/env bash
#>
(
set -u
set -x
set -o pipefail
cd `dirname $0`
export PATH=$PATH:/home/d.belyaev/grep/bike

ff=`find . -name "*.pids"`
for f in $ff; do fork -pf=$f -kila -d; done

)#>>"$0.log" 2>&1
